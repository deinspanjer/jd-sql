# Project build/test outputs (jd-sql uses out/ for all generated artifacts)
out

# Gradle local caches and state (should never be committed)
.gradle/

# Note: keep Gradle Wrapper scripts and binaries versioned
# (gradlew, gradlew.bat, gradle/wrapper/**) – required for reproducible builds